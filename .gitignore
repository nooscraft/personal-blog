# Zola build output
public/
sitemap.xml

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# Cursor (local editor config, never committed)
.cursor/

# Node (local tooling for generators)
node_modules/
package-lock.json
npm-debug.log*

# Cover images are tracked (illo/Volt set). CI still skips Replicate when a PNG exists.
# Uncomment to stop tracking generated covers again:
# static/images/covers/*.png
# static/images/covers/*.jpg

# Local env files (API keys)
.env
.env.*

